version: '3'

services:
   archimage:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    command: /bin/bash 
    volumes:
      - /home/jg/scripts:/data

volumes:
  data:
